# ============================
# ReportPortal configuration
# ============================
# rp.enable is FALSE by default. Set to true only when RP infrastructure
# is available. Override at runtime via: -Drp.enable=true
# The pipeline always passes -Drp.enable=false to ensure CI never blocks.

rp.endpoint = https://your.reportportal.server/api/v1
rp.uuid     = <your_reportportal_api_token>
rp.launch   = KC-SDET-Automation
rp.project  = kc-sdet-automation

rp.enable      = true
rp.description = KC SDET Automation Execution
rp.attributes  = type:automation;framework:cucumber;team:QA

# Optional
rp.convertimage  = true
rp.skipped.issue = false
