# Maven-filtered at build time — ${...} tokens are replaced by the active Maven properties.
# Copied to target/allure-results/ by maven-resources-plugin before tests run.
Browser=${browser}
Environment=${env}
RunMode=${runMode}
Threads=${threads}
Headless=${headless}
