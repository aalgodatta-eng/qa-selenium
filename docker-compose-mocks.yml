
services:
  wiremock:
    image: wiremock/wiremock:3.6.0
    container_name: kc-wiremock
    ports:
      - "8089:8080"
    volumes:
      - ./tests/src/test/resources/wiremock:/home/wiremock
    command: ["--verbose"]
